[project]
name = "where-do-we-stand"
version = "1.0.0"
description = "Сервис генерации шапки «Где мы находимся»"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
filterwarnings = ["ignore::DeprecationWarning"]
